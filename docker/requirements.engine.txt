# CosyVoice v2/v3 runtime dependencies (pinned to upstream versions).
# Torch / torchaudio are installed separately in the Dockerfile from the
# official PyTorch index. UI/training/optional-acceleration packages from
# upstream CosyVoice/requirements.txt are intentionally omitted:
#   * gradio, matplotlib, tensorboard, fastapi-cli, rich — demo/dev only
#   * fastapi, uvicorn, pydantic — supplied by requirements.api.txt
#   * grpcio, grpcio-tools — upstream gRPC service we do not expose
#   * openai-whisper, wget, gdown — dataset/prep tooling
#   * deepspeed, tensorrt-cu12* — optional accelerators (opt-in rebuild)
--extra-index-url https://aiinfra.pkgs.visualstudio.com/PublicPackages/_packaging/onnxruntime-cuda-12/pypi/simple/
conformer==0.3.2
diffusers==0.29.0
HyperPyYAML==1.2.3
hydra-core==1.3.2
inflect==7.3.1
librosa==0.10.2
lightning==2.2.4
modelscope==1.20.0
networkx==3.1
numpy==1.26.4
omegaconf==2.3.0
onnx==1.16.0
onnxruntime-gpu==1.18.0
protobuf==4.25
pyarrow==18.1.0
pyworld==0.3.4
transformers==4.51.3
wetext==0.0.4
x-transformers==2.11.24
huggingface_hub>=0.23
hf_transfer>=0.1.6
